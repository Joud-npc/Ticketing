<Window x:Class="Ticketing.Views.AdminLoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:commands="clr-namespace:Ticketing.Commands"
        Title="Espace Utilisateur" Height="600" Width="400"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <TabControl>
            <TabItem Header="Connexion">
                <StackPanel Margin="20" VerticalAlignment="Center">
                    <TextBlock Text="Email :" />
                    <TextBox x:Name="EmailTextBox" Margin="0,5">
                        <TextBox.Text>
                            <Binding Path="Email" UpdateSourceTrigger="PropertyChanged" ValidatesOnDataErrors="True" ValidatesOnExceptions="True">
                                <Binding.ValidationRules>
                                    <commands:HogwartsEmailValidationRule />
                                </Binding.ValidationRules>
                            </Binding>
                        </TextBox.Text>
                    </TextBox>
                    <TextBlock Text="Email doit se terminer par @gryffondor.hp, @poufsouffle.hp, @serdaigle.hp ou @serpentard.hp" 
                               FontSize="10" Foreground="Gray" Margin="0,2,0,5"/>

                    <TextBlock Text="Mot de passe :" Margin="0,10,0,0"/>
                    <PasswordBox x:Name="PasswordBox" Margin="0,5"/>

                    <TextBlock x:Name="ErrorMessage" Foreground="Red" Visibility="Collapsed" Margin="0,10"/>

                    <Button Content="Connexion" Click="OnLoginClicked" Margin="0,10" Height="30"/>
                    <Button Content="Retour" Click="OnRetourClicked" Margin="0,10" Height="30"/>
                </StackPanel>
            </TabItem>

            <TabItem Header="Créer un compte">
                <StackPanel Margin="20" VerticalAlignment="Center">

                    <TextBlock Text="Nom :" />
                    <TextBox x:Name="CreateNomTextBox" Margin="0,5"/>

                    <TextBlock Text="Prénom :" />
                    <TextBox x:Name="CreatePrenomTextBox" Margin="0,5"/>

                    <TextBlock Text="Email :" />
                    <TextBox x:Name="CreateEmailTextBox" Margin="0,5">
                        <TextBox.Text>
                            <Binding Path="NewEmail" UpdateSourceTrigger="PropertyChanged" ValidatesOnDataErrors="True" ValidatesOnExceptions="True">
                                <Binding.ValidationRules>
                                    <commands:HogwartsEmailValidationRule />
                                </Binding.ValidationRules>
                            </Binding>
                        </TextBox.Text>
                    </TextBox>
                    <TextBlock Text="Email doit se terminer par @gryffondor.hp, @poufsouffle.hp, @serdaigle.hp ou @serpentard.hp" 
                               FontSize="10" Foreground="Gray" Margin="0,2,0,5"/>

                    <TextBlock Text="Mot de passe :" Margin="0,10,0,0"/>
                    <PasswordBox x:Name="CreatePasswordBox" Margin="0,5"/>

                    <TextBlock Text="Confirmer le mot de passe :" Margin="0,10,0,0"/>
                    <PasswordBox x:Name="ConfirmPasswordBox" Margin="0,5"/>

                    <TextBlock Text="Rôle :" Margin="0,10,0,0"/>
                    <ComboBox x:Name="RoleComboBox" Margin="0,5">
                        <ComboBoxItem Content="Admin" IsSelected="True"/>
                        <ComboBoxItem Content="Utilisateur"/>
                    </ComboBox>

                    <TextBlock x:Name="CreateErrorMessage" Foreground="Red" Visibility="Collapsed" Margin="0,10"/>

                    <Button Content="Créer le compte" Click="OnCreateAccountClicked" Margin="0,10" Height="30"/>
                    <Button Content="Retour" Click="OnRetourClicked" Margin="0,10" Height="30"/>
                </StackPanel>
            </TabItem>
        </TabControl>
    </Grid>
</Window>