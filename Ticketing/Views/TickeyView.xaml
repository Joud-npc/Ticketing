<Window x:Class="Ticketing.Views.TicketView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Ticketing.Views"
        xmlns:commands="clr-namespace:Ticketing.Commands"
        Title="Envoyé un hibout" Height="450" Width="400"
        WindowStartupLocation="CenterScreen">
    
    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="Titre" />
            <TextBox Text="{Binding Titre}" Margin="0,5"/>

            <TextBlock Text="Description" />
            <TextBox Text="{Binding Description}" Margin="0,5" Height="60" AcceptsReturn="True"/>

            <TextBlock Text="Nom" />
            <TextBox Text="{Binding Nom}" Margin="0,5"/>

            <TextBlock Text="Prénom" />
            <TextBox Text="{Binding Prenom}" Margin="0,5"/>

            <TextBlock Text="Email" />
            <TextBox Margin="0,5">
                <TextBox.Text>
                    <Binding Path="Email" UpdateSourceTrigger="PropertyChanged" ValidatesOnDataErrors="True" ValidatesOnExceptions="True">
                        <Binding.ValidationRules>
                            <commands:HogwartsEmailValidationRule />
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <TextBlock Text="Email doit se terminer par @gryffondor.hp, @poufsouffle.hp, @serdaigle.hp ou @serpentard.hp" 
                       FontSize="10" Foreground="Gray" Margin="0,2,0,5"/>

            <TextBlock Text="Catégorie" />
            <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding Categorie}" Margin="0,5"/>

            <Button Content="Envoyer" Command="{Binding EnvoyerCommand}" Margin="0,10"/>
            
            <Button Content="Retour" Click="OnRetourClicked" Width="100" Margin="-200,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>